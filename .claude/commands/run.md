Build and run the iOS app on simulator.

1. If `project.yml` exists, run `xcodegen generate` first
2. Use XcodeBuildMCP to build and launch on iOS Simulator
3. If build fails, report errors and attempt fixes
4. Confirm the app is running

Target: iOS Simulator (iPhone 16 Pro preferred)
