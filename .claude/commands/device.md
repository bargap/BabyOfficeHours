Build and run the iOS app on a physical device.

1. Use XcodeBuildMCP to list connected devices
2. If multiple devices, ask which one to target
3. Build for the selected device
4. Deploy and launch the app
5. Report any code signing or provisioning issues

Note: Requires valid provisioning profile and signing certificate.
