Build the iOS app for simulator.

1. If `project.yml` exists, run `xcodegen generate` first
2. Use XcodeBuildMCP to build for iOS Simulator
3. Report any build errors clearly
4. If there are errors, attempt to fix them and rebuild

Target: iOS Simulator (iPhone 16 Pro preferred)
